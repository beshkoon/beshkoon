install: true
script:
- curl -o sunvox-1.9.4c.zip http://www.warmplace.ru/soft/sunvox/sunvox-1.9.4c.zip
- ls -a
- unzip -l sunvox-1.9.4c.zip
- ls -a
#- tar czf sunvox.tar.gz sunvox/linux
#- ls -ahl
#- find docs/en -type f -size +1024k -exec ls -lh '{}' \;
#- du -chs docs/en/*


deploy:
  skip_cleanup: true
  provider: releases
#  user: "beshkoon"
#  password: ""
  api_key: "${test2}"
  file: "../sunvox.tar.gz"
#  on:
#    tags: true
