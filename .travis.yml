install: true
script:
- git clone https://github.com/textbrowser/dooble
- ls -a
- cd dooble
- ls -a
- tar czf ../dooble.tar.gz --exclude "2.x/Windows*"  --exclude "2.x/Translations*"  --exclude "2.x/Dictionaries*"  2.x/
- ls -lh ../dooble.tar.gz

deploy:
  skip_cleanup: true
  provider: releases
#  user: "beshkoon"
#  password: ""
  api_key: "${test2}"
  file: "../dooble.tar.gz"
#  on:
#    tags: true
